interface:
  display_name: "dingxinglizi"
  short_description: "On-demand, resumable multi-agent software delivery"
  default_prompt: "Use $software-project-orchestrator to initialize or safely resume this project, route only the current stage's minimum roles, and deliver it with independent QA."
